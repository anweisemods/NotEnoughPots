# 1.12 port: @Mod(version = ...) needs a compile time constant, so FML reads the version from here
# instead -- without it it warns on every start and falls back to the mcmod.info metadata
${mod_id}.version=${version}
